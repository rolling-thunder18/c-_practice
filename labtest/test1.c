#include<stdio.h>
int main(){
    char c[100];
    char *p;
    fgets(c,100,stdin);
    p=c;
    
int i=0;
    
while(){
    
        if((p==c|| *(p-1)==' ')&&(*(p+1)!=' '||*p!='\n')){
            p+i=
            printf(" %c ", *p);
        }
        p++;

    }
    return 0;
}